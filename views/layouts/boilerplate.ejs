<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TripConnect <% if (title) { %> - <%= title %> <% } %></title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/rating.css">

    <style>
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            min-height: 100vh;
        }

        .auth-fullscreen {
            min-height: 100vh;
            background: linear-gradient(rgba(44, 62, 80, 0.9), rgba(44, 62, 80, 0.7)),
                        url('https://images.unsplash.com/photo-1488646953014-85cb44e25828?auto=format&fit=crop&w=1470&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow: hidden;
        }

        .auth-card {
            background: white;
            border-radius: 20px;
            padding: 60px 50px;
            max-width: 520px;
            width: 100%;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
            text-align: center;
        }

        .auth-logo {
            font-size: 3.8rem;
            font-weight: 800;
            color: #2c3e50;
        }

        .auth-card .btn {
            background: #fe424d;
            border: none;
            border-radius: 50px;
            padding: 16px;
            font-size: 1.2rem;
        }
    </style>
</head>
<body>

    <%- include('../includes/navbar.ejs') %>

    <% if (title === 'Login' || title === 'Sign Up') { %>
        <div class="auth-fullscreen">
            <%- body %>
        </div>
    <% } else { %>
        <div class="my-4">
            <%- include('../includes/flash.ejs') %>
            <%- body %>
        </div>
        <!-- Footer hata diya -->
    <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/script.js"></script>
</body>
</html>